---
"oportet": patch
---

The default relay URL (`Transport.relayUrls.prod`) is now `https://id.oportet.xyz`. `relay.onramp.xyz` still serves the same relay, so code that pins the old URL keeps working.
