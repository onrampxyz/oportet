---
"oportet": minor
---

Removed `Transport.relayUrls.stg`. It pointed at RISE's staging relay (stg.relay.wallet.risechain.com), which oportet does not run. The relay URLs are now `anvil` and `prod`.
